# v36.6.1 ORDER ISSUE HOTFIX — οδηγός εγκατάστασης

## Γιατί απαιτείται

Ο έλεγχος της v36.6.0 σε **πραγματικό** σχήμα βάσης (πλήρες `00_FULL_INSTALL`
και οι 12 migrations με τη σειρά) εντόπισε τρία σφάλματα που δεν φαίνονταν
στις υπάρχουσες δοκιμές, επειδή εκείνες χτίζουν δικό τους απλοποιημένο σχήμα:

| # | Σφάλμα | Τι σταματούσε |
|---|--------|----------------|
| 1 | Λείπει η στήλη `mo_orders.issued_by`, ενώ τη γράφει η `save_order_atomic` | Κάθε αποθήκευση δελτίου, ακόμη και πρόχειρου |
| 2 | Το `mo_orders_status_check` δεν δεχόταν την κατάσταση `issued` | Κάθε έκδοση δελτίου |
| 3 | Το `saved_versions_action_check` δεν δεχόταν `copy` / `import` / `cancel_lock` | Αντιγραφή αιτήματος, εισαγωγή Excel, ακύρωση μελέτης |

Επιπλέον διορθώνονται:

- Η εποπτική εικόνα προσμετρούσε **ακυρωμένες** μελέτες, ενώ ο έλεγχος ορίου
  τις εξαιρεί. Ο επόπτης έβλεπε μεγαλύτερο δεσμευμένο ποσό από το πραγματικό.
- Η πλήρης διαγραφή έσβηνε το `app_audit_log` χωρίς να καταγράφει την ίδια τη
  διαγραφή. Πλέον το ιστορικό **διατηρείται** και γράφεται εγγραφή
  `study_purged` με α/α μελέτης, καθαρή αξία, χρήστη και τους αριθμούς των
  δελτίων που αφαιρέθηκαν.

---

## Σειρά εκτέλεσης

Για staging που βρίσκεται ήδη στη **v36.6.0**, εκτελούνται μόνο δύο αρχεία:

1. `supabase/migrations/202608070001_fix_order_issue_and_purge_audit.sql`
2. `supabase/105_VERIFY_ORDER_ISSUE_HOTFIX.sql` — **αυστηρά αναγνωστικό**

Και οι 12 έλεγχοι του δεύτερου αρχείου πρέπει να επιστρέψουν `✅ ΟΚ`.
Έπειτα αναρτάται το νέο `index.html`.

Αν το περιβάλλον δεν έχει ακόμη τη v36.6.0, προηγείται κανονικά ολόκληρη η
σειρά του `STAGING_INSTALL_ORDER.md` και μετά η παραπάνω migration.

> Η migration είναι **επαναλήψιμη** και εκτελείται σε μία συναλλαγή. Ελέγχει
> πρώτα ότι δεν υπάρχουν δελτία ή αποθηκευμένες εκδόσεις με μη αναγνωρίσιμη
> τιμή και σταματά χωρίς αλλαγές αν βρει.

---

## Αλλαγές στο frontend

Το `index.html` περνά σε **v36.6.1** και απαιτεί σχήμα βάσης **36.6.1**.

- `loadOverview()`: προστέθηκε `record_status = 'active'`, ώστε η εποπτική
  εικόνα να συμφωνεί με τον έλεγχο ορίου.
- Νέα `MO.followUnit()`: η Δημοτική Ενότητα που είναι επιλεγμένη στις
  Προμήθειες/Υπηρεσίες ακολουθεί τον χρήστη στα Δελτία.
- Η ακύρωση κλειδωμένης μελέτης ενημερώνει πλέον και την ενότητα Δελτίων,
  όπως έκαναν ήδη το κλείδωμα και η πλήρης διαγραφή.
- **Επανυπολογίστηκαν τα SHA-256 hashes της Content Security Policy.** Χωρίς
  αυτό ο browser θα απέρριπτε τα δύο inline script blocks.

Απαιτείται ολόκληρος ο φάκελος `vendor`, όπως και στις προηγούμενες εκδόσεις.

---

## Έλεγχος μετά την εγκατάσταση

1. Ως διαχειριστής, κλείδωμα μελέτης σε μία Δ.Ε.
2. Δημιουργία ανάθεσης και **αποθήκευση πρόχειρου δελτίου** — δούλευε ποτέ
   πριν; Όχι. Πρέπει να πετύχει τώρα.
3. **Έκδοση** του δελτίου· ο αριθμός έχει μορφή `ΔΥ-ΕΤΟΣ-ΔΕ-ΑΑΑ`
   (ή `ΔΕ-…` για Υπηρεσίες).
4. Μετάβαση σε «Απεσταλμένο» και «Παραληφθέν».
5. **Ακύρωση** κλειδωμένης μελέτης με αιτιολογία — δούλευε ποτέ πριν; Όχι.
6. **Αντιγραφή** αιτήματος σε άλλη Δ.Ε. — δούλευε ποτέ πριν; Όχι.
7. Έλεγχος ότι το υπόλοιπο της ομάδας των Δ.Ε. συμφωνεί ανάμεσα στη λωρίδα
   ορίου, στο «Ιστορικό ομάδας» και στα pills των Δ.Ε. του επόπτη.
8. Πλήρης διαγραφή δοκιμαστικής μελέτης και επαλήθευση ότι στο
   `app_audit_log` υπάρχει εγγραφή `study_purged`.

---

## Αυτοματοποιημένες δοκιμές

```sh
npm ci
npm test
```

**43/43** επιτυχείς. Προστέθηκε το `tests/real-schema-e2e.test.mjs`, το οποίο
εγκαθιστά το **πραγματικό** installer και όλες τις migrations και εκτελεί
πλήρη κύκλο: κατανομή ομάδων → κλείδωμα → ανάθεση → πρόχειρο → έκδοση →
παραλαβή → πλήρης διαγραφή, καθώς και έλεγχο ορίου ομάδας με ακύρωση και
αντιγραφή αιτήματος. Αυτή η δοκιμή είναι που εντόπισε το τρίτο σφάλμα.

---

## Εκκρεμότητες πριν από την παραγωγή

- Το `index.html` δείχνει στο **staging** (`omncqldgtkdcjpqfwwlr.supabase.co`).
  Πριν από την ανάρτηση σε παραγωγή αλλάζουν `SUPABASE_URL`, `SUPABASE_KEY`
  **και** οι αντίστοιχες διευθύνσεις `connect-src` της CSP.
- Τα `technical_specs` των ομάδων Υπηρεσιών (SRV01–SRV08) παραμένουν κενά.
- Οι ιστορικές τιμές του καταλόγου Υπηρεσιών δεν αποτελούν τεκμηριωμένη
  έρευνα αγοράς και επιβεβαιώνονται πριν από δημοσίευση πραγματικής μελέτης.
