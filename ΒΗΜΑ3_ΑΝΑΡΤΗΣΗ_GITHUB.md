# Βήμα 3 — Ανάρτηση στο GitHub Pages

Ο κώδικας δεν έχει ανέβει ακόμη πουθενά. Το παρόν είναι η πλήρης διαδικασία,
με έμφαση στα δύο σημεία που σπάνε σιωπηλά αν παραλειφθούν.

---

## 3.1 Καθαρισμός πριν από το πρώτο commit

Στη ρίζα υπάρχουν βοηθητικά αρχεία που δεν πρέπει να δημοσιευθούν:

```
apply_v3661.py   colcheck.mjs   e2e.mjs   verify_hotfix.mjs
```

Διαγράψτε τα ή προσθέστε τα στο `.gitignore`. Το `.gitignore` χρειάζεται
συμπλήρωση:

```gitignore
node_modules/
*.log
.DS_Store

# προσωρινά εργαλεία ανάπτυξης
apply_v*.py
colcheck.mjs
e2e.mjs
verify_hotfix.mjs
specs_export.csv
```

Ο φάκελος `vendor/` **πρέπει** να ανέβει — η CSP απαγορεύει εξωτερικά CDN και
η εφαρμογή δεν ξεκινά χωρίς αυτόν.

Προσθέστε **κενό αρχείο `.nojekyll`** στη ρίζα. Χωρίς αυτό το GitHub Pages
τρέχει Jekyll και αγνοεί φακέλους που ξεκινούν με κάτω παύλα.

---

## 3.2 Οι δύο αλλαγές που πρέπει να γίνουν μαζί

Αυτό είναι το κρίσιμο σημείο. Η αλλαγή του Supabase project απαιτεί
**δύο** επεμβάσεις στο `index.html`, όχι μία.

**α) Τα διαπιστευτήρια** (γραμμή ~938):

```js
const SUPABASE_URL='https://ΝΕΟ_REF.supabase.co';
const SUPABASE_KEY='ΝΕΟ_ANON_KEY';
```

**β) Το `connect-src` της CSP** (γραμμή 6) — δύο εμφανίσεις, `https` και `wss`:

```
connect-src https://ΝΕΟ_REF.supabase.co wss://ΝΕΟ_REF.supabase.co ...
```

> Αν αλλάξετε μόνο το (α), η εφαρμογή θα φορτώσει κανονικά και μετά **κάθε**
> κλήση προς τη βάση θα μπλοκάρεται από τον browser. Στην κονσόλα θα
> εμφανιστεί `Refused to connect ... violates Content Security Policy`.
> Είναι η πιο συνηθισμένη αιτία «λευκής οθόνης» μετά από αλλαγή project.

**Το `script-src` δεν αλλάζει** — εκτός αν επεξεργαστείτε τον κώδικα, οπότε
πρέπει να ξαναϋπολογιστούν τα δύο SHA-256 hashes.

---

## 3.3 Ποιο project θα χτυπάει;

Έχετε τρία:

| Project | ref | Κατάσταση |
|---|---|---|
| staging | `omncqldgtkdcjpqfwwlr` | ✅ v36.6.1, καθαρό, 0 δελτία |
| v2 | `mtzhnrupaytsdpfydqta` | αντίγραφο δεδομένων, άγνωστη έκδοση σχήματος |
| παραγωγικό | `hafaxrebjzootjzzqkzx` | παλαιά γενιά, με πραγματικά δεδομένα |

**Πρόταση**: η πρώτη ανάρτηση να δείχνει στο **staging**, με το αποθετήριο
**private**. Έτσι δοκιμάζετε τη ροή GitHub Pages χωρίς κανένα ρίσκο σε
πραγματικά δεδομένα. Η στροφή σε άλλο project είναι μετά μια αλλαγή δύο
γραμμών.

Το anon key δεν είναι μυστικό — προστατεύεται από RLS. Δεν υπάρχει κίνδυνος
από τη δημοσίευσή του, **εφόσον** το RLS είναι ενεργό, πράγμα που
επιβεβαιώθηκε στους ελέγχους.

---

## 3.4 Εντολές

```sh
cd <φάκελος του project>
git init
git add .
git commit -m "v36.6.1 — order issue hotfix, installer fix, real-schema tests"
git branch -M main
git remote add origin https://github.com/vasilis1730-web/promitheies-rodou.git
git push -u origin main
```

Έπειτα: **Settings → Pages → Source: Deploy from a branch → main / (root)**.

Η σελίδα ζωντανεύει σε 1–2 λεπτά στο
`https://vasilis1730-web.github.io/promitheies-rodou/`.

---

## 3.5 Έλεγχος μετά την ανάρτηση

1. Άνοιγμα της διεύθυνσης σε **ανώνυμο παράθυρο** — αποφεύγει την τοπική cache.
2. `F12` → καρτέλα Console. Δεν πρέπει να υπάρχει κανένα μήνυμα CSP.
3. Κάτω δεξιά: **v36.6.1 ORDER ISSUE HOTFIX**.
4. Σύνδεση, κλείδωμα δοκιμαστικής μελέτης, έκδοση δελτίου.
5. Καρτέλα Network: οι κλήσεις προς `…supabase.co/rest/v1/…` επιστρέφουν
   `200`, όχι `403`. Ένα `403` σημαίνει έλλειψη `GRANT` στους ρόλους, όχι
   πρόβλημα RLS.

---

## 3.6 Δύο πράγματα που δεν πρέπει να ξεχαστούν

- **Cache του GitHub Pages**: μετά από κάθε νέο push, `Ctrl+F5`. Το
  `index.html` σερβίρεται με μακρύ cache και οι χρήστες θα βλέπουν την παλιά
  έκδοση για ώρες.
- **Το `REQUIRED_SCHEMA_VERSION` είναι έλεγχος ισότητας.** Αν αναβαθμίσετε τη
  βάση χωρίς να ανεβάσετε νέο `index.html`, η εφαρμογή **δεν ξεκινά**
  καθόλου. Η σειρά είναι πάντα: πρώτα η migration, αμέσως μετά το push.
